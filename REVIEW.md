# FLP 不可能性定理

## 摘要

FLP 研究完全异步消息系统中的二元共识。系统允许至多一个进程停止，消息不会丢失，但进程速度和消息延迟都没有上界。论文证明：任何满足一致性和非平凡性的确定性协议，都有一条合法执行始终不能作出决定。因此，这类协议无法对所有合法执行同时保证安全性和终止性。

证明围绕“二价配置”展开。作者先找到一个初始二价配置，再说明任意指定事件都可以在不破坏二价性的条件下执行，最后用轮转调度把这些有限步骤接成无限执行。论证的难点不在二价性的定义，而在于最后得到的执行不能靠丢消息或饿死进程维持。

## 问题和结论

论文从分布式事务提交谈起。参与同一事务的进程需要一致选择提交或中止；如果某个进程迟迟没有响应，其余进程既不能确定它已经停止，也不能确定它只是很慢。等待可能使系统一直没有结果，提前决定又可能忽略尚未到达的信息。

FLP 把这个问题抽象成二元共识。每个进程有一个 0 或 1 的初始值，协议的正确性分为三项：

- 一致性（agreement）：任何可达配置都不能同时包含决定 0 和决定 1。
- 非平凡性（nontriviality）：0 和 1 都必须能在某些可达配置中成为决定值。
- 终止性（termination）：每条合法执行最终都至少有一个进程决定。

前两项在论文中合称 partial correctness；再满足终止性，协议才是在一个故障下 totally correct。这里的非平凡性弱于现在常见的 validity，终止性也只要求至少一个进程决定。论文在较弱条件下已经得到不可能性，所以加强这些要求不会使结论失效。

主定理的量词容易被误读。准确说法是：对每一个候选协议，都存在一条不决定的合法执行；不是该协议的每条执行都会失败。协议是确定性的，只表示给定配置和事件后，下一配置唯一。调度器仍可选择下一步由哪个进程执行、接收哪条消息，因此同一协议可以产生许多不同执行。

## 系统模型

系统至少包含两个确定性进程。每个进程保存输入、本地状态和一个只能写一次的输出，进程之间只通过消息通信。已发送但尚未接收的消息放在全局缓冲区中。

事件记作 `(p, m)`：进程 `p` 接收消息 `m`，完成一次原子计算，并可发送有限条新消息。`m` 也可以是空值；论文允许进程在缓冲区仍有待收消息时执行一次空接收。配置由所有进程的本地状态和缓冲区中的消息共同组成。给定配置和一个可执行事件，后继配置唯一。

一串可依次执行的事件称为 schedule，它实际产生的步骤序列称为 run。对无限 run 而言，执行无限多步的进程是 nonfaulty process，只执行有限步的进程是 faulty process。一条 run 合法（admissible）需要满足：

1. 至多一个进程是 faulty process；
2. 每条发给 nonfaulty process 的消息最终都被接收。

所以，完全异步不等于消息可以丢失。调度器可以把消息延迟任意长的有限时间，却不能永远扣住发给正常进程的消息。后面的反例必须满足这两个条件，否则只能说明不公平调度会使协议停滞。

## 二价性和事件交换

证明先反设存在一个 totally correct 的协议。对任意可达配置 `C`，考察从 `C` 出发可能到达的决定值：

- 只能到达决定 0 的配置称为 0-valent；
- 只能到达决定 1 的配置称为 1-valent；
- 两种决定都仍有可能的配置称为 bivalent。

在 total correctness 的反设下，每个可达配置都至少有一种可能的决定值，因此上面的分类覆盖所有可达配置。

“尚未决定”和“二价”并不相同。一个配置虽然还没有输出，但如果后续只能决定 0，它已经是 0-valent。反过来，已经包含决定值的配置一定单价，因为一致性不允许后续出现另一个决定值。

单价性还会被后继继承。若 `C` 是 0-valent，而它的某个后继能够决定 1，那么 `C` 本身也能沿这条路径决定 1，与定义矛盾。最后构造无限执行时，这一性质用来排除“中途已经单价、轮末又恢复二价”的可能。

证明还使用 Lemma 1 的交换性质。若两段 schedule 涉及的进程集合互不相交，调换它们的先后顺序会到达同一配置。不同进程的步骤只修改各自的本地状态；对消息缓冲区而言，两种顺序删除和加入的是同一批消息。这个结论不能直接用于同一进程的两个事件，因为前一步会改变后一步读到的本地状态。

## Lemma 2：初始二价配置存在

反设所有初始配置都是单价的。由非平凡性，至少有一个初始配置最终能够决定 0，也有一个能够决定 1；在反设下，它们分别是 0-valent 和 1-valent。

把所有进程的输入逐个从第一个配置改成第二个配置，可以得到一条初始配置链。链上相邻配置只相差一个进程的输入，而两端的价不同，所以中间必有一对相邻配置 `C0`、`C1`：`C0` 是 0-valent，`C1` 是 1-valent，两者只在进程 `p` 的输入上不同。

从 `C0` 出发，让 `p` 永远不执行，同时公平调度其他进程并投递发给它们的消息。这仍是一条 admissible run，因为只有 `p` 停止。由 total correctness，某个其他进程最终必须决定。取截至并包含第一次决定的有限 schedule，记为 `σ`。

`σ` 不含 `p` 的步骤，因此也可以从 `C1` 执行。除 `p` 外，其余进程在两次执行中有相同的初始状态，接收相同消息，经历相同状态转移，最后会作出同一个决定。若决定值是 0，就与 `C1` 的 1-valent 性质冲突；若决定值是 1，就与 `C0` 的 0-valent 性质冲突。初始配置不可能全部单价。

这一步实际使用的是不可区分性：当唯一持有不同输入的进程从未运行时，其余进程无法知道它的输入。

## Lemma 3：指定事件执行后仍可保持二价

给定二价配置 `C` 和一个当前可执行事件 `e`。记 `𝒞` 为从 `C` 出发、不执行 `e` 所能到达的配置集合，再记

`𝒟 = {e(E) | E ∈ 𝒞}`。

如果 `e` 接收一条具体消息，只要没有执行 `e`，该消息就仍在缓冲区；若 `e` 是空接收，它始终可执行。因此 `e` 对 `𝒞` 中每个配置都适用。Lemma 3 要证明 `𝒟` 中至少有一个二价配置。

反设 `𝒟` 中的配置全都单价。因为 `C` 二价，从它出发既能到达 0-valent 配置，也能到达 1-valent 配置。考察到达这些配置的 schedule：若 schedule 不含 `e`，就在末尾补一次 `e`；若已经含有 `e`，取第一次执行 `e` 后的配置。利用单价性会被后继继承，可以得到 `𝒟` 中既有 0-valent 配置，也有 1-valent 配置。

沿一条从 `C` 出发且不含 `e` 的有限 schedule，逐步比较每个配置执行 `e` 后的价。在某个相邻位置，价必然从 0 变为 1，或从 1 变为 0。于是存在相邻配置 `C0` 和 `C1 = e'(C0)`，使

`D0 = e(C0)` 为 0-valent，`D1 = e(C1)` 为 1-valent。

接下来分两种情况。

若 `e` 与 `e'` 属于不同进程，Lemma 1 允许交换二者。从 `C0` 先执行 `e` 再执行 `e'`，与先执行 `e'` 再执行 `e` 会到达同一配置。前一条路径从 0-valent 的 `D0` 出发，终点只能是 0-valent；后一条路径的终点却是 1-valent 的 `D1`，矛盾。

若 `e` 与 `e'` 都属于进程 `p`，二者不能直接交换。此时从 `C0` 出发暂停 `p`，取一段不含 `p`、但已经包含首次决定的有限 schedule `σ`，其终点记为 `A`。`σ` 可以与属于 `p` 的事件交换。由此，从 `A` 执行 `e` 可以到达一个 0-valent 配置，从 `A` 依次执行 `e'`、`e` 可以到达一个 1-valent 配置，所以 `A` 应当是二价的。但 `A` 已经包含决定，按一致性又必须单价，仍然矛盾。

两个分支都不成立，`𝒟` 中必有二价配置。也就是说，给定任意当前可执行事件，总能先安排一段有限 schedule，再以该事件收尾，并让最终配置保持二价。

## 无限 admissible 非决定执行

Lemma 2 给出初始二价配置，Lemma 3 负责延长一次。还需要选择目标事件，使无限次延长不会忽略某个进程或某条消息。

论文维护一个进程队列，初始顺序任意。每轮取队首进程 `p`。如果轮次开始时有发给 `p` 的待收消息，就选择其中发送最早的一条；否则选择空接收。将这个接收动作作为目标事件，用 Lemma 3 找到一段以它结尾、终点仍二价的有限 schedule。完成后把 `p` 移到队尾。

每轮至少执行目标事件，所以无限多轮确实组成一条无限 run。进程数有限且持续轮转，每个进程都会无限多次成为队首，也就执行无限多步。构造中实际上没有 faulty process。

消息也不会被永久延迟。取一条发给 `p` 的消息 `x`。`x` 发出时，排在它之前的旧消息只有有限条。每次 `p` 到达队首，只要 `x` 尚未收到，就会移除当前最早的待收消息；在 `x` 之后产生的消息不会排到它前面。经过有限多轮，`x` 必然被选中。

因此，这条 run 满足 admissibility。每轮终点都保持二价，而单价配置不可能重新变成二价，所以轮内也没有配置进入单价，更没有进程已经决定。最终得到一条合法、无限且不决定的 run，与 total correctness 的终止性要求冲突。

## 评价与边界

这篇论文是反例执行没有依赖明显不公平的调度。最终构造中每个进程都运行无限多步，每条消息也都会收到，失败只发生在终止性上。论文还允许可靠通信、无界本地存储和较强的广播能力，因此结论很难归因于某个过弱的通信机制。二价性也是证明中最有影响的方法。直观上，Lemma 3 把这种直觉变成一个可以反复使用的有限延伸。最后的轮转调度再补上公平性，整条反例才闭合。

定理描述的是最坏情况：每个协议都有一条合法执行不终止，而不是协议在通常情况下无法工作。它针对确定性、完全异步的消息传递模型，也不评价具体实现的平均性能。后来的研究通过修改前提获得进展，例如 partial synchrony 增加最终可用的时间界，随机化协议把确定性终止改为概率 1 终止，failure detector 则为进程提供额外的故障信息。因此，使用 FLP 分析实际系统时，应先列出协议采用的时间假设、故障信息和终止条件。

## 参考文献

1. Michael J. Fischer, Nancy A. Lynch, and Michael S. Paterson. “Impossibility of Distributed Consensus with One Faulty Process.” *Journal of the ACM* 32(2), 1985, 374–382. [DOI](https://doi.org/10.1145/3149.214121)
2. Cynthia Dwork, Nancy Lynch, and Larry Stockmeyer. “Consensus in the Presence of Partial Synchrony.” *Journal of the ACM* 35(2), 1988, 288–323. [DOI](https://doi.org/10.1145/42282.42283)
3. Michael Ben-Or. “Another Advantage of Free Choice: Completely Asynchronous Agreement Protocols.” *Proceedings of the 2nd ACM Symposium on Principles of Distributed Computing*, 1983, 27–30. [DOI](https://doi.org/10.1145/800221.806707)
4. Tushar Deepak Chandra and Sam Toueg. “Unreliable Failure Detectors for Reliable Distributed Systems.” *Journal of the ACM* 43(2), 1996, 225–267. [DOI](https://doi.org/10.1145/226643.226647)
