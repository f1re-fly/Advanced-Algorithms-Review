# FLP 不可能性：论文梳理与证明评述

> 主文献：Fischer、Lynch、Paterson，*Journal of the ACM* 32(2), 1985, 374–382

## 摘要

FLP 论文讨论的是一个条件十分明确的问题：在完全异步的消息传递系统中，如果协议是确定性的，并且允许至多一个进程停止，那么共识是否还能对所有合法执行保证终止？论文的答案是否定的。它证明，对于任何满足一致性和非平凡性的确定性共识协议，调度器都能安排出一条符合系统模型、却始终无法作出决定的无限执行。

这个结论经常被概括成“异步系统无法达成共识”，但这种说法过于宽泛。FLP 并没有否认共识在许多实际执行中能够完成，也没有构造出不同进程决定不同值的安全性错误。它指出的是，只要完全异步、确定性和停止故障这几个条件同时存在，就不能要求协议在每一条合法执行上都终止。

证明的核心是二价性。作者先说明至少存在一个初始配置，使决定 0 和决定 1 都仍有可能；再证明，无论当前准备执行哪个事件，总能先安排一段有限执行，使该事件发生后系统依然保持二价。不断重复这一过程，就能得到一条永远没有决定、同时又满足进程和消息公平性的无限执行。

## 1. 从提交问题到共识问题

论文以分布式事务提交作为背景。多个节点共同参与一次事务时，它们需要对“提交”或“中止”形成一致意见。真正困难的地方不只是让所有节点输出同一个结果，还要在节点可能停止、消息可能长时间延迟的情况下保证最终得出结果。

FLP 将这类问题抽象为二元共识。每个进程有一个取值为 0 或 1 的初始输入，协议需要满足 agreement、nontriviality 和 termination。Agreement 要求所有已经作出决定的进程选择同一个值。Nontriviality 要求 0 和 1 都有机会成为某次执行的决定结果，从而排除“无论输入是什么都固定输出 0”之类没有实际意义的协议。论文中的 nontriviality 比许多现代教材采用的 validity 条件更弱，因此不可能性结论并不是由过强的有效性要求造成的。

Termination 要求每一条 admissible run 最终都出现决定。论文把 agreement 和 nontriviality 合称为 partial correctness；如果一个协议还满足 termination，就称它在一个故障下 totally correct。主定理可以表述为：任何满足 partial correctness 的确定性共识协议，都存在一条没有任何进程作出决定的 admissible run，所以这样的协议不可能在一个停止故障下 totally correct。

这里需要特别注意结论中的量词。论文说的是“对每个候选协议，都能找到一条失败执行”，而不是“一个候选协议的所有执行都会失败”。协议虽然是确定性的，执行过程却不是唯一的。给定一个配置和一个事件，后继配置确实唯一；但在同一配置下，调度器仍可选择下一步让哪个进程运行，以及向它交付哪条消息。FLP 利用的是这种调度次序的不确定性，而不是进程内部的随机行为。

## 2. 系统模型

系统至少包含两个确定性进程。每个进程有自己的输入、本地状态、只写一次的输出和无界存储，进程之间只能通过消息通信。已经发送但尚未接收的消息保存在全局消息缓冲区中。

一次事件写作 `(p, m)`，表示进程 `p` 接收消息 `m`，随后完成一个原子步骤。这里的 `m` 可以是缓冲区中的某条具体消息，也可以是空值。按照论文的模型，空接收始终允许，即使缓冲区中仍有等待接收的消息。进程执行这一步时可以改变自己的本地状态，也可以向其他进程发送有限条消息。配置则记录某一时刻所有进程的内部状态以及缓冲区中的全部在途消息。

能够从某个配置依次执行的一串事件称为 schedule；这些事件实际产生的状态变化构成 run。对一条无限 run 而言，如果某个进程只执行了有限步，它就是 faulty process；如果它执行了无限步，它就是 nonfaulty process。一条 run 要成为 admissible run，需要满足两个条件：其中至多有一个 faulty process，而且所有发给 nonfaulty process 的消息最终都会被接收。

因此，“完全异步”不等于允许丢失消息。调度器可以让某个进程非常慢，也可以把某条消息推迟任意长的有限时间，却不能永远扣住发给正常进程的消息，更不能让两个或更多进程永久停止。后面的反例构造必须遵守这些限制，否则它只能说明一个不公平的调度会阻塞协议，并不能证明 FLP 的结论。

这一模型也解释了停止故障为什么棘手。一个长时间没有响应的进程，可能已经停止，也可能只是运行得很慢，还可能是它的消息仍在路上。系统中没有时钟上界可供参考，其他进程仅凭等待时间无法区分这几种情况。

## 3. 二价性与事件交换

证明采用反证法，先假设存在一个在一个停止故障下 totally correct 的确定性协议。在这个假设下，从每个可达配置出发都至少能够继续到某个决定结果；如果存在一个配置，从它出发永远无法决定，那么 termination 已经被直接违反。

对任意配置 `C`，考虑从 `C` 出发仍可能到达的决定值。如果只能到达决定 0 的状态，就称 `C` 为 0-valent；如果只能到达决定 1 的状态，就称它为 1-valent；如果两种决定都仍然可能，则称它为 bivalent。

二价和“当前还没有决定”并不是同一个概念。一个尚未决定的配置可能已经是单价的，因为后续无论怎样调度，它都只会决定同一个值。二价更强，它表示系统尚未越过决定 0 与决定 1 之间的分界。反过来，只要某个配置中已经有进程作出决定，agreement 就会使所有后续决定只能取同一个值，因此该配置一定是单价的。

单价性还有一个重要性质：一旦出现，就不会在后继配置中消失。假设 `C` 是 0-valent，而它的某个后继仍能到达决定 1，那么从 `C` 也能沿同一条路径到达决定 1，这与 `C` 的定义矛盾。正因为单价不可逆，只要每一轮构造的终点仍是二价，就能断定这一轮中途也没有作出决定。

证明还要使用事件交换性质，也就是论文的 Lemma 1。如果两段 schedule 涉及的进程集合互不相交，那么它们可以调换次序，最后到达同一个配置。最常用的情形是两个事件分别属于不同进程：两步各自只修改对应进程的本地状态；对消息缓冲区而言，它们只是取走各自接收的消息，再加入各自产生的消息，所以执行顺序不会改变最终结果。若两个事件属于同一个进程，这个结论一般不成立，因为前一步会改变后一步读取的本地状态。

有了这些准备，整篇证明可以看成三个连续的问题：初始时是否一定存在二价配置；到达二价配置后，能否在执行指定事件的同时继续保持二价；如果每次只能得到一小段有限执行，又怎样把这些片段接成一条真正 admissible 的无限 run。

## 4. 初始二价配置为什么一定存在

Lemma 2 证明至少有一个初始配置是二价的。反设所有初始配置都是单价的。由 nontriviality，必然存在一个最终可以决定 0 的初始配置，也存在一个最终可以决定 1 的初始配置。在当前反设下，前者是 0-valent，后者是 1-valent。

将所有进程的输入逐个从前一个配置改成后一个配置，可以得到一串初始配置，其中相邻两个配置只相差一个进程的输入。这串配置的起点和终点具有不同的价，因此沿途一定能找到一对相邻配置 `C0` 和 `C1`：`C0` 是 0-valent，`C1` 是 1-valent，并且它们只在进程 `p` 的输入上不同。

现在从 `C0` 出发安排一条执行，让 `p` 从不运行，同时公平地调度其余进程并投递消息。这样只有 `p` 是 faulty process，所以该执行仍然符合 admissibility。按照反证开头对 total correctness 的假设，执行最终必须出现决定。取截至首次决定的有限前缀，记为 `σ`。

由于 `σ` 中没有 `p` 的步骤，它也可以从 `C1` 出发执行。除 `p` 外，所有进程在两次执行中具有相同的初始状态，接收相同的消息，也经历相同的事件，因此会作出同一个决定。如果这个决定是 0，就与 `C1` 的 1-valent 性质矛盾；如果决定是 1，就与 `C0` 的 0-valent 性质矛盾。

所以“所有初始配置都是单价的”这一假设不能成立，初始二价配置必然存在。这一步的关键其实是不可区分性：当唯一拥有不同输入的进程始终不运行时，其他进程没有办法知道那一位输入究竟是什么。

## 5. 如何在执行指定事件后继续保持二价

Lemma 3 是证明中最关键、也最容易读乱的一步。它要说明：给定一个二价配置 `C` 和一个当前可执行事件 `e`，总能先执行一段不含 `e` 的有限 schedule，最后再执行 `e`，并使得到的新配置仍然是二价的。

从 `C` 出发，考虑所有不执行 `e` 就能到达的配置，把这个集合记为 `𝒞`。再对其中每个配置执行 `e`，得到集合 `𝒟 = {e(E) | E ∈ 𝒞}`。如果 `e` 接收的是一条具体消息，那么在尚未执行 `e` 时，这条消息仍留在缓冲区中；如果 `e` 是空接收，它本来就始终可执行。因此，`e` 对 `𝒞` 中的每个配置都适用。

目标是证明 `𝒟` 中至少有一个二价配置。反设 `𝒟` 中所有配置都是单价的。因为 `C` 是二价的，从它出发既能到达 0-valent 配置，也能到达 1-valent 配置。考察通往其中某个配置的 schedule：如果这段 schedule 没有执行过 `e`，就在末尾补上 `e`；如果它执行过 `e`，就取第一次执行 `e` 之后的配置。结合单价性不会在后继中改变这一事实，可以推出 `𝒟` 中既有 0-valent 配置，也有 1-valent 配置。

`e(C)` 本身属于 `𝒟`。在 `𝒞` 中选一个配置 `E`，使 `e(E)` 与 `e(C)` 的价不同。沿着一条从 `C` 到 `E`、且不含 `e` 的 schedule 逐步观察，`e` 执行后的价必然在某个相邻位置发生变化。因此可以找到相邻配置 `C0` 和 `C1`，其中 `C1 = e'(C0)`，而 `D0 = e(C0)` 与 `D1 = e(C1)` 分别具有相反的价。不妨设 `D0` 是 0-valent，`D1` 是 1-valent。

如果 `e` 和 `e'` 属于不同进程，Lemma 1 允许交换它们的顺序。从 `C0` 先执行 `e` 再执行 `e'`，与先执行 `e'` 再执行 `e` 会到达同一配置。可是前一种顺序从 0-valent 的 `D0` 出发，终点只能保持 0-valent；后一种顺序的终点正是 1-valent 的 `D1`。同一个配置不可能同时具有两种价，因此这种情况不成立。

如果 `e` 和 `e'` 都属于进程 `p`，两者不能直接交换。这时从 `C0` 出发暂停 `p`，公平调度其他进程。由 total correctness，系统最终仍会作出决定。截取截至首次决定的有限 schedule `σ`，并把它的终点记为 `A`。由于 `σ` 不含 `p` 的事件，它可以与 `e` 以及连续的 `e'、e` 调换先后次序。

从 `D0` 执行 `σ` 所到达的配置，等同于从 `A` 再执行 `e`，所以它是 `A` 的一个 0-valent 后继。类似地，从 `D1` 执行 `σ` 所到达的配置，等同于从 `A` 依次执行 `e'` 和 `e`，所以它是 `A` 的一个 1-valent 后继。这说明从 `A` 出发仍有可能决定 0，也有可能决定 1，即 `A` 是二价的。但 `A` 已经包含首次决定，按照 agreement 又必须是单价的，矛盾。

两种情况都导出矛盾，因而最初的反设错误，`𝒟` 中一定存在二价配置。换句话说，调度器总能把当前指定的事件留到一段有限 schedule 的最后，并且在执行它之后不失去二价性。Lemma 3 本身只给出一段有限延伸，但它可以被反复使用。

## 6. 从有限延伸得到无限执行

最后还需要解决公平性问题。只说“每次挑一个能保持二价的事件”是不够的，因为调度器可能永远忽略某个进程或某条消息，那样得到的执行未必 admissible。

论文采用轮转方式安排进程。先将所有进程排成一个固定顺序。每一轮取队首进程 `p`：如果本轮开始时有消息等待 `p` 接收，就选择其中发送时间最早的一条；如果没有，就选择一次空接收。把这个接收步骤记为目标事件 `e`。根据 Lemma 3，可以找到一段以 `e` 结尾、执行后仍保持二价的有限 schedule。完成这一轮后，把 `p` 移到队尾，再处理下一个进程。

每一轮至少执行目标事件，因此所有轮次接在一起会形成一条无限 run。由于进程数量有限且采用轮转顺序，每个进程都会反复成为队首，并在对应轮次中至少执行一次。于是每个进程都执行无限多步，实际构造中甚至没有进程成为 faulty process。

消息最终也不会被永久推迟。任取一条发给进程 `p` 的消息 `x`。在 `x` 发出时，排在它之前的旧消息只有有限条。此后每当 `p` 轮到队首，只要 `x` 尚未被接收，就会接收当前最早的待收消息。排在 `x` 前面的消息数量因此不断减少，不可能无限减少而始终不到零。经过有限多轮，`x` 必然会成为最早消息并被接收。

整条 run 因而满足 admissibility：所有进程都持续执行，所有发给它们的消息也最终得到接收。另一方面，每轮的终点都由 Lemma 3 保持二价。由于单价配置不可能重新变成二价，轮次中间也不可能出现单价配置，更不可能已经有进程作出决定。于是这是一条 admissible、无限且始终不决定的 run，与 total correctness 要求的 termination 正面矛盾，主定理得证。

## 7. 对结论的理解

FLP 最有价值的地方，不只是给出了一个不可能性结论，还准确指出了困难来自哪里。在完全异步模型中，延迟没有上界，因此“很慢”和“已经停止”无法区分。只要协议必须容忍一个停止故障，它就不能永远等待某个进程；但如果它准备在没有该进程信息的情况下作出决定，调度器又可能利用尚未交付的事件保留另一种决定可能。二价性把这种直觉变成了可以逐步维持的证明对象。

这个结果没有否认实际系统能够完成共识。现实中的协议通常会改变 FLP 的某个前提。例如，系统可以在部分同步假设下使用超时和领导者机制，可以借助故障检测器获得额外信息，也可以引入随机性，把“每条执行都确定终止”改成概率意义上的终止。工程实现有时在正常网络条件下进展很快，而在极端调度下暂时失去活性，这同样不与 FLP 冲突。

论文也没有证明共识协议一定破坏 agreement。它构造的坏执行恰恰一直停留在决定之前，失败的是 termination。类似地，证明不依赖消息丢失、不需要两个进程同时崩溃，也没有靠永久饿死正常进程取巧。最终构造出的执行满足论文自己给出的 admissibility 条件，这正是结论有力度的原因。

还应注意 transaction commit 与共识的边界。提交问题是论文的重要动机，但正式证明面对的是抽象的二元共识协议。因此，不能把 FLP 简化成“某个提交算法会卡住”；它讨论的是一整类确定性协议。如果某个工程方案声称避开了 FLP，真正需要检查的是它是否引入了额外的时间假设、故障信息、随机性，或者削弱了终止保证。

## 8. 结语

读完 FLP 后，最值得记住的不是一句笼统的“异步共识不可能”，而是证明如何把一次有限的犹豫延长成无限的不决定。Lemma 2 提供二价的起点，Lemma 3 保证任意指定事件都能在不打破二价性的情况下被执行，最后的轮转调度则确保进程和消息都没有被不公平地忽略。调度器每次只延长一小段合法前缀，但这些前缀可以无穷地接下去。

因此，FLP 的准确含义是：在完全异步的消息传递系统中，只要协议是确定性的，并要求容忍一个停止故障，就无法同时保留共识的安全性条件和对所有 admissible runs 的终止保证。它不是说分布式共识毫无可能，而是说明任何可行方案都必须清楚地回答一个问题：它究竟放宽了 FLP 的哪项前提。

## 参考文献

Fischer, Michael J., Nancy A. Lynch, and Michael S. Paterson. “Impossibility of Distributed Consensus with One Faulty Process.” *Journal of the ACM* 32(2), 1985, 374–382.
