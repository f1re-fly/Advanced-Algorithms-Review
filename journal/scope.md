## FLP

FLP 研究在通信延迟没有上界、允许一个进程停止的条件下，多个进程能否保证达成共识。

## 准备看的重点

FLP 的结论：协议不能保证所有合法执行都终止，但共识仍可能在某些执行中达成。

证明部分重点检查三点：初始二价配置为什么存在；关键事件为什么可以延后且保持二价；构造出的无限执行为什么既是 admissible，又不进入决定状态。

相关研究只在结论边界中简要提到“增加时间假设”“增加故障信息”和“引入随机性”，不展开成综述。主稿仍以复原原论文第 374–380 页的模型和证明为主。
